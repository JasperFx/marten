namespace Marten.Patching
{
    public enum RemoveAction
    {
        /// <summary>
        /// Remove the first occurrence
        /// </summary>
        RemoveFirst,

        /// <summary>
        /// Remove all occurrences
        /// </summary>
        RemoveAll
    }
}
